# Pixel Workbench Design Notes

The current redesign uses two generated references as visual direction only:

- `pixel-workbench-desktop.png`
- `pixel-workbench-mobile.png`

These images define the tone: hard-edged panels, graphite background, phosphor green, amber, and cyan accents, compact monospace metadata, and old-monitor screenshot frames.

Do not treat text, project descriptions, dates, links, or screenshots inside the generated mockups as factual content. Project content should come from each selected repository's README, docs, manifests, GitHub metadata, and live or local inspection.
